{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/RDS/spec-id/master/assets/schema_input.json",
  "title": "RDS/spec-id pipeline - params.input schema",
  "description": "Schema for the file provided with params.input",
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "sample": {
        "type": "string",
        "pattern": "^\\S+$",
        "errorMessage": "Sample name must be provided and cannot contain spaces",
        "meta": [
          "id"
        ]
      },
      "assembly": {
        "type": "string",
        "format": "file-path",
        "exists": true,
        "pattern": "^([\\S\\s]*\\/)?[^\\s\\/]+\\.f(ast)?a(\\.gz)?$",
        "errorMessage": "Fasta file for assembly must be provided, cannot contain spaces and must have extension '.fa.gz' or '.fasta.gz'"
      },
      "checkm2": {
        "type": "string",
        "format": "file-path",
        "pattern": "^([\\S\\s]*\\/)?[^\\s\\/]+\\.tsv$",
        "errorMessage": "CheckM2 must be a file must be a tsv."
      }
    },
    "required": [
      "sample",
      "assembly"
    ]
  }
}
